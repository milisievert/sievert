# directive

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build directive` to build the library.

## Running unit tests

Run `nx test directive` to execute the unit tests via [Vitest](https://vitest.dev/).
