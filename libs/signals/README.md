# signals

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build signals` to build the library.

## Running unit tests

Run `nx test signals` to execute the unit tests via [Vitest](https://vitest.dev/).
